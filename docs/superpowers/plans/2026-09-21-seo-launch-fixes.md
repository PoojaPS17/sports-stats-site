# SEO Launch Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Google must see only `https://sports-db.live` as the site, every indexable page must return a correct status, canonical and metadata, and the crawlable HTML must be cacheable and light enough for a first crawl of ~125k URLs.

**Architecture:** Next.js 16 app router, self-hosted on an Oracle VM behind Cloudflare (`src/proxy.ts` is the request gate; `SITE_LAUNCHED=1` switches the holding page off). Each task changes one concern. Where a page's indexing rule changes, the sitemap query gets the SQL twin of the same rule with a test (the existing pattern: `src/lib/sitemap.ts` `playedSql` + `tests/sitemap-players.test.ts`).

**Tech Stack:** Next.js 16.3 (app router; breaking changes vs older Next, read `node_modules/next/dist/docs/` for anything you are unsure about, AGENTS.md says so), TypeScript, node:test via `npx tsx --test tests/*.test.ts`, pg.

**Spec:** the audit `/private/tmp/claude-501/-Users-ps-Claude-sports-stats-site/e456d8e9-cead-4299-a57f-cdfaa22e52a1/scratchpad/seo-audit.md` (findings 1.1, 1.2, 1.4, 1.5, 1.8, 2.1, 2.3, 2.4, 2.2, 3.1, 5.1, 5.2, 6.2, 6.7, 8.1, 8.3, 8.4, 8.5 are in scope; read the section for each item you implement; it has file:line and the audit's proposed fix). The user's instruction: "We need to make sure google sees only sports-db.live which is our main site."

## Global Constraints

- The one canonical site is `https://sports-db.live` (`src/lib/site.ts` SITE_URL, from `NEXT_PUBLIC_SITE_URL`). Nothing in this plan may make another host indexable.
- Never open or read any `.env*` file. There is no env in this worktree and no database access; tests use the embedded Postgres harness the existing tests use. Never run a scraper or any script against a real database or real ESPN.
- Nothing is pushed. Commit after each task; end every commit message with `Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>`.
- Data accuracy is the site's overriding requirement: never change what a page's figures say. This plan changes indexing, headers, caching, markup weight only. A rendered page must show the same data as before.
- Tooling: `export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"`; tests `npx tsx --test tests/*.test.ts`; `npx tsc --noEmit` (a `SITE_NAME` error there is pre-existing; `rm -rf .next/dev` if stale errors, `npx next typegen` if a LayoutProps error appears); `npx eslint src tests` (scripts/ is not covered by eslint). No Grep tool; use `git grep -nE`. macOS `sed -i` fails, use the Edit tool. Never bare `git stash`.
- Do not touch the Cloudflare dashboard, the VM, DNS or Vercel settings; the user does those. If a fix needs one, say so in your report.
- Every indexing rule change (noindex on a page) must be mirrored in that page type's sitemap query and covered by a test that would fail if they disagreed.

---

### Task 1: Only sports-db.live is indexable (hosts, robots, meta robots)

**Files:** Modify `src/proxy.ts`, `src/app/robots.ts`, `src/lib/metadata.ts`, `src/app/calendar/[league]/route.ts`, `src/app/calendar/[league]/[slug]/route.ts` (or wherever the two calendar routes are), `src/app/layout.tsx` (only the `robots` object if needed). Tests: `tests/proxy.test.ts` (new; import the default proxy function and call it with `NextRequest` objects), `tests/metadata.test.ts` (new or extend).

- [ ] **1.1 (audit 1.1) Non-primary hosts are never indexable.** In `src/proxy.ts`, a request whose host does not match `OWN_DOMAIN` (the vercel.app review copy, any other alias) gets `x-robots-tag: noindex, nofollow` on every response, and `/robots.txt` on such a host returns `User-agent: *\nDisallow: /\n` with `x-robots-tag: noindex, nofollow` and `cache-control: no-store`. The host must still serve the page normally (it is a review copy), except that localhost / 127.0.0.1 / dev hosts must keep working: treat `localhost`, `127.0.0.1` and `*.localhost` as development hosts and leave them unchanged (no noindex needed). The main domain after launch (`SITE_LAUNCHED=1`) is unchanged and adds no headers. Keep the matcher as is. Tests: vercel host gets the header and the robots body; localhost gets neither; sports-db.live with SITE_LAUNCHED=1 gets neither; sports-db.live before launch keeps today's holding-page behaviour.
- [ ] **1.2 (audit 1.2)** The pre-launch robots response (`Disallow: /`) also sets `cache-control: no-store` (so Cloudflare cannot cache it for hours after launch). Test it.
- [ ] **1.3 (audit 2.3) www redirect.** `www.sports-db.live` (any path, query kept) gets a 308 redirect to `https://sports-db.live` + same path and query, before any other proxy behaviour, both before and after launch. Test it.
- [ ] **1.4 (audit 1.5) meta robots.** `pageMeta` in `src/lib/metadata.ts` must always return a `robots` object so the root's `max-image-preview:large` / `max-snippet:-1` hint is not dropped: indexable pages `{ index: true, follow: true, googleBot: { index: true, follow: true, "max-image-preview": "large", "max-snippet": -1 } }`; `noindex` pages keep `noindex, follow` semantics as today (read the current code and keep exactly what it does for noindex). Test both, and that every existing noindex caller still produces noindex.
- [ ] **1.5 (audit 1.4) `/search` and `/calendar`.** Remove `/search` from the `disallow` list in `src/app/robots.ts` (the page already sends `noindex, follow`; a robots-blocked linked page can be indexed URL-only). Add `X-Robots-Tag: noindex` to both calendar route responses and drop `/calendar/` from the disallow list. Keep `/api/` disallowed. Update/extend the robots test if one exists.
- [ ] Run tests, tsc, eslint. Commit: `feat: only sports-db.live is indexable (host gate, www redirect, robots)`.

### Task 2: Correct status codes, canonicals and structured data

**Files:** Modify `src/app/[league]/scores/[date]/page.tsx`, `src/app/[league]/players/page.tsx`, `src/app/layout.tsx`, `src/app/page.tsx`, `src/lib/structuredData.ts`, `src/app/contact/page.tsx`, `src/app/[league]/matchweek/page.tsx` (plus `src/lib/sitemap.ts`, `src/components/LeagueSubNav.tsx` for the hub); add `src/app/not-found.tsx`, `src/app/error.tsx`, `public/logo-512.png`; delete the unused create-next-app SVGs in `public/` (`file.svg globe.svg next.svg vercel.svg window.svg`, only after `git grep` shows nothing references them). Tests as noted.

- [ ] **2.1 (audit 2.2) Invalid dates 404.** `/nba/scores/2025-13-45` and `/nba/scores/2025-02-30` must `notFound()` (in both `generateMetadata` and the page), like `src/app/tennis/scores/[date]/page.tsx:26`: shape check plus a real-date check (`new Date(`${d}T12:00:00Z`)` valid AND `toISOString().slice(0,10) === d`). Put the check in a small pure function with a unit test (valid date, month 13, Feb 30, Feb 29 leap and non-leap, garbage).
- [ ] **2.2 (audit 2.1) Canonicals.** `/[league]/players` passes its path `/${league}/players` to `pageMeta`. Remove `alternates: { canonical: "/" }` from the root layout so no page can inherit the home canonical by accident, and give `src/app/page.tsx` its own `metadata` (or `generateMetadata`) with canonical `/`. Then `git grep` every `pageMeta(` and `generateMetadata` and list, in your report, any indexable page that still ends up with no canonical (fix each; a noindex page needs none). Add a test that iterates `pageMeta` behaviour with and without a path.
- [ ] **2.3 (audit 5.1) Organization logo.** `src/lib/structuredData.ts:28` points at `/icon.png` (404 today). Generate a square PNG of at least 512x512 for `public/logo-512.png` (derive it from the existing site icon/brand: read `src/app/icon.*` / `apple-icon.*` / `opengraph-image.*`, how they are produced, and produce a clean 512px square from the same design; use `sharp` only if it is already in node_modules, else render with the same generator the icon routes use, e.g. `next/og` ImageResponse in a one-off local script, and commit only the resulting PNG). Reference `${SITE_URL}/logo-512.png`. Test that the structured-data logo path corresponds to a file that exists under `public/`.
- [ ] **2.4 (audit 5.2)** Remove the duplicate BreadcrumbList on `/contact` (`src/app/contact/page.tsx:17` vs `LegalPage.tsx` -> `Breadcrumbs.tsx`); keep one.
- [ ] **2.5 (audit 1.8)** Add `src/app/not-found.tsx` (site chrome via the layout, a short message, a link to home and to each sport hub, a link to search; it must send a 404 status, which `not-found.tsx` does by itself) and `src/app/error.tsx` (client component, retry button, link home). Keep them plain and free of data queries.
- [ ] **2.6 (audit 2.4) Matchweek hub.** `/[league]/matchweek` (and `/nfl/week`, `/ucl/matchday` through the existing rewrites) currently `redirect()`s with a 307 but is in the sitemap and the sub-nav. Read `src/app/[league]/matchweek/page.tsx`, the season/week routes and `src/lib/sitemap.ts:81`, then make the hub crawl-clean without changing which data any page shows: preferred fix, render the hub as a real page whose content is the current season's week index (reuse the existing `WeekIndex` component/data the target page uses) with a self-canonical; if that is not feasible without duplicating content, use `permanentRedirect` (308) to the same target AND remove the hub URL from the sitemap and point the sub-nav at the target. State which you chose and why. Test the sitemap side (hub URL absent or present according to the choice).
- [ ] Run tests, tsc, eslint. Commit per item where natural.

### Task 3: Head-to-head pages with no meetings are not indexable

**Files:** Modify `src/app/[league]/h2h/[pair]/page.tsx`, `src/lib/sitemap.ts` (h2h sitemap, around lines 207-218), `src/lib/analytics.ts` only if needed. Tests: extend `tests/sitemap-players.test.ts`'s approach in a new `tests/sitemap-h2h.test.ts`.

- [ ] Read how `getHeadToHead` (`src/lib/analytics.ts` ~368-398) decides `meetings`, and how the h2h sitemap builds all C(n,2) pairs. A pair page with `meetings === 0` gets `noindex, follow` in `generateMetadata` (keep the page itself rendering, it is still a valid page for users), and its description never says "0-0-0 in 0 meetings" (use a neutral description for that case). The h2h sitemap lists only pairs that have at least one completed game between them, computed by the SQL twin of the page's `meetings` definition (same completed rule, same league, both orders of home/away; check whether the page counts stage-specific games and make the twin identical). Test with the embedded Postgres harness: a pair with a completed game is listed, a pair with none is not, a pair with only an uncompleted or postponed game is not (if the page's rule counts it, the twin must too; say which). The test must fail if the SQL and the page rule diverge (assert both on the same fixtures).
- [ ] Run tests, tsc, eslint. Commit.

### Task 4: Entity pages become cacheable (static params) without changing what they show

**Files:** every page/layout with a dynamic segment below `[league]` and under `/cricket`, `/tennis`, `/f1` (list in audit 8.1); `src/app/[league]/players/[slug]/page.tsx` (the cricket `split` `searchParams`). Tests: a test asserting the presence of the export where practical, plus the build evidence below.

- [ ] First read `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/generate-static-params.md`, `.../03-api-reference/03-file-conventions/route-segment-config` and `node_modules/next/dist/docs/01-app/02-guides/cdn-caching.md` (or the closest equivalents) and record in your report the Next 16 rule that decides whether a dynamic route is cached. The audit observed on the Vercel copy that `/nba/teams/los-angeles-lakers`, `/nba/players/lebron-james`, `/nba/games/<id>`, `/nba/standings/2024`, `/epl/matchweek/1` answer `cache-control: private, no-cache, no-store` although the segment files export `revalidate`.
- [ ] For each dynamic-segment page and its `opengraph-image`, add `export function generateStaticParams() { return []; }` (must return an array, per the docs), keeping `dynamicParams` at its default so unknown slugs still render on demand and 404 through `notFound()` exactly as today. PRESERVE each page's existing `revalidate` value; do not invent new ones. In your report give a table: route, existing revalidate (or none), static-params added yes/no. List every page that has NO `revalidate` export: do not guess one, leave it and flag it.
- [ ] The player page awaits `searchParams` only for the cricket `split` (`players/[slug]/page.tsx` ~117-121), which makes every player page dynamic. Change it so the NBA/NFL/soccer player pages do not read `searchParams`, without changing what a cricket player page shows for any `?split=` value. Decide the mechanism by reading the code (options: a path segment `/ipl/players/x/splits/opponent` with a 308 from the old query form; or a client-side tab that keeps the query out of the server render). The canonical URL of a cricket player page stays the bare page; the `?split=` URLs must keep working for existing inbound links. State your choice and why. Other pages that read `searchParams` (`compare`, `injuries`, `top-games`, `f1`, `f1/standings`) stay dynamic; list them.
- [ ] Verify with a real production build: `npx next build` from this worktree (there is no env, so if the build needs a database or env to prerender something that today is fine, record exactly which page and why, and do not work around it by touching env files). Paste the route table (○ / ● / ƒ markers) for the routes above, before (`git stash` is forbidden: build once at the base commit `6f1fcae` in a temporary `git worktree add /tmp/seo-base 6f1fcae` with the same symlinked node_modules, then remove that temp worktree) and after. If the build cannot run at all without env, say so and give what you verified by reading instead.
- [ ] Run tests, tsc, eslint. Commit.

### Task 5: Lighter HTML on the biggest pages

**Files:** `src/components/TeamLogo.tsx`, `src/components/PlayerStatsShared.tsx`, `src/app/[league]/players/page.tsx`, `src/components/ImageActions.tsx`, `src/components/RelatedLinks.tsx` and other list `<Link>` sites named below. Googlebot only processes the first 2 MB of an HTML response, and the audit measured 3.9 MB (LeBron), 7.3 MB (`/nfl/players`), 9.0 MB (`/ucl/players`), so content past 2 MB is invisible to Google today.

- [ ] **5.1 (audit 8.3)** `TeamLogo`: add `loading="lazy"` and `decoding="async"` by default, with a prop (`priority`) that turns them off for logos that are above the fold (page headers); apply `priority` to the header/hero logos (team header, game header, player header team logo). Do not change alt text.
- [ ] **5.2 (audit 8.3)** Player game-log rows: stop rendering an opponent crest `<img>` per row (`OpponentCell` in `PlayerStatsShared.tsx:63`); show the opponent's name/abbreviation text link only. Nothing else in the row changes (same data, same links).
- [ ] **5.3 (audit 6.2)** `/[league]/players` index: remove the per-player avatar image and add `prefetch={false}` to the list links. Every player stays listed (no player disappears). Target: the largest league's page under 1.5 MB of HTML. There is no database here, so measure it from the code: compute bytes per rendered row from a rendered row string and multiply by the known counts (nfl 6,386 rows, ucl 8,942 links) and report the estimate; if a row is still large, shrink the markup (drop redundant wrappers/attributes) until the estimate meets the target.
- [ ] **5.4 (audit 8.5)** `prefetch={false}` on long list links: `PlayerStatsShared.tsx` row links and `RelatedLinks.tsx`. Do not change short nav links.
- [ ] **5.5 (audit 8.4)** `html-to-image` is imported at the top of `ImageActions.tsx`; load it with `await import("html-to-image")` inside the click handler so it is not in the page bundle. Behaviour of the share/download button is unchanged.
- [ ] Tests where a unit test is meaningful (e.g. `TeamLogo` props via `renderToStaticMarkup`: default has `loading="lazy"`, `priority` does not). Run tests, tsc, eslint. Commit.

### Task 6: Export cards are not duplicated in the page HTML (optional, only if it can be done without risk)

**Files:** `src/components/ImageActions.tsx` and its callers. Every section with `ImageActions` renders its export card off-screen (`ImageActions.tsx:110-114`), so tables appear twice in the DOM plus the RSC payload (LeBron 3.9 MB). Audit 6.7 proposes mounting the card only when the user clicks share/download.

- [ ] Read `ImageActions.tsx` and every caller. If (and only if) the card can be mounted on demand with unchanged output, do it: on click, mount the card, wait for the fonts (`document.fonts.ready`) and for every `<img>` inside it to finish loading or fail, then run the same render call as today, then unmount. If any caller passes the card as a server-rendered child (so it is already in the RSC payload, which mounting on click would not remove), report that and stop: do NOT restructure server components to lazy-load props in this task. There is no browser or database here, so this behaviour cannot be verified visually; say plainly what you verified by reading and what needs a manual click test on the deployed site, and write it in your report under "needs manual test". If you decide not to do it, say why and leave the file unchanged; that is an acceptable outcome.
- [ ] Run tests, tsc, eslint. Commit only if changed.

### Task 7: ESPN's "Team" box-score lines are not players

Found by the NFL stored audit on the production VM: 22 rows in `players` (league nfl, `espn_id` negative such as `-8801`, `-13974`, name ` Team`, slugs `team`, `team--8801`, ...) with 1-4 `player_game_stats` rows each. ESPN lists a team-credited line in some NFL box-score tables as an athlete with a negative id and the name "Team"; `scripts/lib/game-stats.ts` (`extractAmericanSports`) stores it like any player, so the site has 22 fake player pages (`/nfl/players/team`, `/nfl/players/team--8801`, ...) that can sit in the players index, sitemaps, team pages and search. A "Team" line in a GAME box score is real ESPN data and stays there; it must not be a player.

**Files:** `scripts/lib/game-stats.ts`, every query and page that lists players or serves a player profile (`git grep -nE "from players|join players|getAllPlayers|playerSeasons|fetchPlayerLog"` across `src/` and `scripts/`, including `src/lib/queries.ts`, `src/lib/sitemap.ts`, search, leaders, team/top-player/teammate chips, compare, `src/app/[league]/players/[slug]/page.tsx` and `[season]/page.tsx`, the players index). Tests: extend the existing sitemap/players tests and add a loader test.

- [ ] Define one shared predicate for "an ESPN pseudo-athlete" (a player whose `espn_id` starts with `-`): a TS helper (`isPseudoAthleteId(id: string)`) and its SQL twin text (`p.espn_id not like '-%'`), in one module, used by every consumer below. Do NOT add any other rule (a real athlete never has a negative id; do not match on the name).
- [ ] Loader: keep the box-score rows for a pseudo-athlete exactly as stored today (the game page shows ESPN's "Team" line), but never create or update a `players` row for one? Read how the game page and `player_game_stats` joins use `players` first. If the game page needs the `players` row to render the "Team" line, keep the row and hide it everywhere a PLAYER is presented; if it does not, stop inserting them. State which you chose and why, with the code path that decides it. Existing rows are cleaned up by a separate production step the user runs (do not write a migration or a delete script that runs by itself; if a cleanup statement is needed, put it in your report as text, not in the repo).
- [ ] Every player-facing consumer excludes pseudo-athletes with the shared predicate: players index, sitemaps (players and player-seasons), search results, leaders, team top-player / teammate / position-peer chips, compare, h2h player lists, player-profile pages (`/nfl/players/team--8801` must 404 through `notFound()`, and must not be in any sitemap). Produce, in your report, a table of every consumer found and what you did (excluded / not applicable because it cannot return them, with the reason).
- [ ] Tests (embedded Postgres harness like the sitemap tests): a pseudo-athlete with game rows is absent from the player sitemap and the players-index query and the search query, and its profile route 404s (test the query function the page uses returns no profile); a real player next to it is still present. The tests must fail if any consumer drops the predicate.
- [ ] Run tests, tsc, eslint. Commit.
